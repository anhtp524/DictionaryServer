export enum Status {
    active = 'active',
    inactive = 'inactive',
    deleted = 'deleted',
    not_payment = 'not_payment',
    is_paid = 'is_paid',
}

export enum Role {
    admin = 'admin',
    user = 'user',
}
